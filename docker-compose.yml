services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./data:/data
      - ./backend/app:/app/app
    environment:
      DATA_DIR: /data
  web:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - api
